info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/users/login
  body:
    type: json
    data: |-
      {
        "email" : "dadasd@dasdasdd.com",
        "password" : "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
